# Magic Tower v6.1 迭代需求

## 用户反馈的问题清单

### P0 - 必须修复

1. **手机刷新后没更新**
   - 原因：Cloudflare 缓存
   - 解决：在 index.html 添加版本号查询参数 `?v=6.1`，或清除 Cloudflare 缓存

2. **Seed 不应该显示在 UI 上**
   - 当前：代码中有 seedDisplay, seedInput, seedApplyBtn 的引用
   - 期望：后台生成种子，不在 UI 显示（胜利画面分享时才显示）
   - 需要清理 game.js 中的相关代码

3. **血条掉血不真实**
   - 当前：HP 条直接更新数值
   - 期望：掉血动画效果（HP 条平滑减少 + 数字飘红）
   - CSS 已添加 .damage-number 动画，需要 JS 逻辑

4. **英雄方向动画没调整**
   - 当前：hero.png 是 4x4 格式，代码期望 4x12 格式
   - 期望：往左走剑朝左，往右剑朝右，往上背对玩家，往下正对玩家
   - 需要：重新生成 hero.png 精灵图，或修改代码适应当前格式

5. **音效太小被 BGM 覆盖**
   - 当前：BGM_VOLUME = 0.3, SFX_VOLUME = 0.7
   - 期望：音效更明显，或进一步降低 BGM 默认音量

### P1 - 应该完成

6. **中英文切换** ✅ 已完成
   - i18n.js 已实现完整国际化
   - 语言切换按钮已添加

7. **噩梦模式** ✅ 已完成
   - map.js 中 DIFFICULTIES.nightmare 已定义
   - 7 层、敌人 x1.35、商店 x1.5、迷雾、诅咒格

8. **方向键 PNG 转 SVG**
   - 当前：assets/ui/arrow_*.svg 已创建（但风格不匹配）
   - 正确流程：
     1. 用 Codex ImageGeneration 生成像素风 PNG
     2. 转换 PNG 为 SVG
   - 风格要求：金色/棕色主色调，像素艺术，16-bit RPG 风格

---

## 技术实现要点

### 1. 血条掉血动画
```css
.damage-number {
  animation: floatUp 0.8s ease-out forwards;
}

@keyframes floatUp {
  0% { opacity: 1; transform: translateY(0); }
  20% { transform: translateY(-8px) scale(1.2); }
  100% { opacity: 0; transform: translateY(-24px) scale(0.8); }
}
```

### 2. 英雄方向精灵图
- 当前格式：4列 x 4行（256x192）
- 代码期望：4列 x 12行（每个动作有4个方向）
- 解决方案：
  - 方案A：修改 animation.js 适应当前 4x4 格式
  - 方案B：重新生成 4x12 格式的精灵图

### 3. 音量平衡
```javascript
// audio.js - 当前值
const BGM_VOLUME = 0.3;  // BGM 默认 30%
const SFX_VOLUME = 0.7;  // 音效默认 70%

// 建议调整
const BGM_VOLUME = 0.2;  // BGM 降低到 20%
const SFX_VOLUME = 0.85; // 音效提高到 85%
```

---

## 文件修改清单

| 文件 | 改动 | 状态 |
|------|------|------|
| `index.html` | 版本号参数 v=6.1 | 待做 |
| `js/game.js` | 移除 seed UI 代码，添加掉血飘字逻辑 | 待做 |
| `js/audio.js` | 调整 BGM/SFX 音量比例 | 待做 |
| `js/animation.js` | 修复英雄精灵图格式兼容 | 已修改 |
| `css/style.css` | 掉血数字动画 | 已添加 |
| `assets/ui/arrow_*.svg` | 重新生成像素风方向键 | 待做 |

---

## 工作目录
`d:\Programs\myprojects\test_things\magic-tower\`

## Codex 执行命令
```powershell
Set-Location 'd:\Programs\myprojects\test_things\magic-tower'; codex exec --skip-git-repo-check -s danger-full-access -c 'model="gpt-5.5"' -c 'reasoning_effort="xhigh"' "Read V6.1_PLAN.md and implement all pending tasks..."
```
